name: Run Tests
on: pull_request
jobs:
 test:
 runs-on: ubuntu-latest
 steps:
 - uses: actions/checkout@v3
 - name: Install update
 run: apt update -y
 - name: Install dependencies
 run: npm install
 - name: Run tests
 run: npm test
